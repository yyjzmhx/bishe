<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.personalmusicsystem.mapper.ReportTemplateMapper">
    
    <resultMap id="BaseResultMap" type="com.example.personalmusicsystem.entity.ReportTemplate">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="config" property="config"/>
        <result column="is_default" property="isDefault"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>
    
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM report_template WHERE id = #{id}
    </select>
    
    <select id="selectDefault" resultMap="BaseResultMap">
        SELECT * FROM report_template WHERE is_default = 1 AND status = 1 LIMIT 1
    </select>
    
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM report_template ORDER BY is_default DESC, update_time DESC
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO report_template (name, description, config, is_default, status)
        VALUES (#{name}, #{description}, #{config}, #{isDefault}, #{status})
    </insert>
    
    <update id="update">
        UPDATE report_template
        SET name = #{name},
            description = #{description},
            config = #{config},
            is_default = #{isDefault},
            status = #{status}
        WHERE id = #{id}
    </update>
    
    <delete id="deleteById">
        DELETE FROM report_template WHERE id = #{id}
    </delete>
    
    <update id="unsetDefaultOthers">
        UPDATE report_template SET is_default = 0 WHERE id != #{currentId}
    </update>
    
</mapper>

